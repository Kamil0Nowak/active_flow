<article class="card shadow-sm bg-base-200">
  <div class="card-body">
    <h3 class="text-2xl"><%= course.title %></h3>
    <p><%= course.description %></p>
    <%= link_to "View course", course_path(course), class: "btn btn-primary", data: { turbo_frame: "_top" } %>
  </div>
</article>
